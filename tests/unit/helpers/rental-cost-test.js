import { trashCost } from 'super-trashes/helpers/trash-cost';
import { module, test } from 'qunit';

module('Unit | Helper | trash cost');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = trashCost([42]);
  assert.ok(result);
});
